

<isset name="sessionVar" value="${1}" scope = "session"/>

${session.custom.sessionVar}
<iscomment>
    ${pdict.CurrentSession.custom.sessionVar }
    ${request.custmo.sessionVar + 1} Request
    ${sessionVAr} Page
</iscomment>

<iscomment>

<isset name="sessionVar" value="${false}" scope = "session"/>

</iscomment>

<iscomment>

<isset name="sessionVar" value="Hello" scope = "session"/>

</iscomment>